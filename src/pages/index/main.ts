/* eslint-disable @typescript-eslint/no-unsafe-assignment */
import '../../utils/fluentBootstrap';

import { styleNavOnChange } from '../../utils/utils';
// console.log(`home page js loaded`);
styleNavOnChange('home');
