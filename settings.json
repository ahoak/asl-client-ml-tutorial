{
  "metaTitle": "ASL Fingerspelling Model Tutorial",
  "metaDescription": "Train ASL Fingerspelling ML Model using Tensorflowjs",
  "name": "ASL Fingerspelling Model Tutorial 👋",
  "theme": "",
  "avatarImage": "",
  "social": {
    "github": "https://github.com/ahoak/asl-client-ml-tutorial",
    "twitter": "https://twitter.com/amber_hoak"
  },
  "showDataVideo": false,
  "trainTutorialSteps": [
    {
      "step": 1,
      "description": "Load Data",
      "helperText": "Load preprocessed data by pressing the button",
      "name" : "loadData",
      "readOnly": "true"
    },
    {
      "step": 2,
      "description": "Set TF Backend",
      "helperText": "edit function `setTensorFlowBackend()` so that we set tensorflow backend to use webGL (or WASM or CPU).",
      "name" : "setTensorFlowBackend",
      "readOnly": "false"
    },
    {
      "step": 3,
      "description": "Encode and Split Data",
      "helperText": " use pre-built functions to encode data using one-hot encoder and then split data into training and test set.",
      "name": "encodeAndSplitData",
      "readOnly": "true"
    },
    {
      "step": 4,
      "description": "Create Model",
      "helperText": "creating a feed-forward model. We will use 5 layers with relu activation function. Function must return the model.",
      "name": "createModel",
      "readOnly": "false"
    },
    {
      "step": 5,
      "description": "Configure Model",
      "helperText": "edit function configureModel() by using the Tf.compile API. We will use an adam optimizer, and add correct loss function.",
      "name" : "configureModel",
      "readOnly": "false"

    },
    {
      "step": 6,
      "description": "Train Model",
      "helperText": " edit function trainModel() by using the Tf.fit API. Set the following parameters: epochs, batchSize,validationData, callbacks. Hint, you may need to transform your data into tensors ",
      "name" : "trainModel",
      "readOnly": "false"
    },
    {
      "step": 7,
      "description": "Cleanup",
      "helperText": "Remove tensors used for model training ",
      "name" : "cleanupTensors",
      "readOnly": "true"
    },
      {
      "step": 8,
      "description": "Download model files",
      "helperText": "Click the button to download your shiney new model. We will use this in our webapp.",
      "name" : "exportModel",
      "readOnly": "false"
    }
  ],
  "predictSteps": [{
    "step": 1,
    "description": "Import Model",
    "helperText": "Import a pre-trained model to perform inference with."
  }, {
    "step": 2,
    "description": "Extract Hand Joint Positions",
    "helperText": "Extract the hand joint positions from within an image."
  }, {
    "step": 3,
    "description": "Normalize",
    "helperText": "Normalize the joint positions from -1 to 1."
  }, {
    "step": 4,
    "description": "Classify",
    "helperText": "Run inference with the normalized joint positions to get a classification (ASL sign)."
  }, {
    "step": 5,
    "description": "Cleanup",
    "helperText": "Cleanup the tensors used while infering."
  }, {
    "step": 6,
    "description": "Predict",
    "helperText": "Run prediction on the model"
  }]
}